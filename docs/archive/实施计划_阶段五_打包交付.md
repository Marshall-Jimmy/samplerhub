# Jima'sSamplerHub - 实施计划：阶段五（打包与交付）

> 预计耗时：2天 | 目标：打包应用、生成文档、最终交付

---

## 任务清单

### Task 1: electron-builder 配置

**目标：** 配置打包脚本，生成 Windows 和 macOS 安装包

**文件：** `electron-builder.yml`

**步骤：**

- [ ] **Step 1: 创建 electron-builder 配置**
  ```yaml
  appId: com.jima.samplerhub
  productName: "Jima's SamplerHub"
  copyright: Copyright © 2024 Jima
  directories:
    output: dist-electron
    buildResources: build
  files:
    - dist/main/**
    - dist/renderer/**
    - dist/preload/**
    - package.json
  extraResources:
    - from: samplerhub.db
      to: samplerhub.db
  asar: true
  asarUnpack:
    - "**/*.{node,dll}"
  
  # Windows 配置
  win:
    target:
      - target: nsis
        arch:
          - x64
          - ia32
      - target: portable
        arch:
          - x64
    icon: build/icon.ico
    publisherName: Jima
    verifyUpdateCodeSignature: false
  
  nsis:
    oneClick: false
    allowToChangeInstallationDirectory: true
    createDesktopShortcut: always
    createStartMenuShortcut: true
    shortcutName: "Jima's SamplerHub"
    uninstallDisplayName: "Jima's SamplerHub"
    license: LICENSE.txt
    artifactName: "${productName}-Setup-${version}-${arch}.${ext}"
  
  portable:
    artifactName: "${productName}-Portable-${version}-${arch}.${ext}"
  
  # macOS 配置
  mac:
    target:
      - target: dmg
        arch:
          - x64
          - arm64
      - target: zip
        arch:
          - x64
          - arm64
    icon: build/icon.icns
    category: public.app-category.music
    hardenedRuntime: true
    gatekeeperAssess: false
    entitlements: build/entitlements.mac.plist
    entitlementsInherit: build/entitlements.mac.plist
  
  dmg:
    title: "${productName} ${version}"
    artifactName: "${productName}-${version}-${arch}.${ext}"
    contents:
      - x: 130
        y: 220
      - x: 410
        y: 220
        type: link
        path: /Applications
  
  # Linux 配置（可选）
  linux:
    target:
      - AppImage
      - deb
      - rpm
    icon: build/icons
    category: AudioVideo
    maintainer: Jima
    vendor: Jima
    synopsis: "智能采样管理工具"
    description: "为音乐制作人打造的智能采样管理工作站"
  ```

- [ ] **Step 2: 创建图标资源**
  ```bash
  # 创建图标目录
  mkdir -p build/icons
  
  # 需要准备的图标文件：
  # - build/icon.ico (Windows, 256x256)
  # - build/icon.icns (macOS, 1024x1024)
  # - build/icons/16x16.png
  # - build/icons/32x32.png
  # - build/icons/48x48.png
  # - build/icons/64x64.png
  # - build/icons/128x128.png
  # - build/icons/256x256.png
  # - build/icons/512x512.png
  ```

- [ ] **Step 3: 创建 macOS 权限配置**
  ```xml
  <!-- build/entitlements.mac.plist -->
  <?xml version="1.0" encoding="UTF-8"?>
  <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
  <plist version="1.0">
    <dict>
      <key>com.apple.security.cs.allow-jit</key>
      <true/>
      <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
      <true/>
      <key>com.apple.security.cs.allow-dyld-environment-variables</key>
      <true/>
      <key>com.apple.security.cs.disable-library-validation</key>
      <true/>
      <key>com.apple.security.automation.apple-events</key>
      <true/>
      <key>com.apple.security.files.user-selected.read-write</key>
      <true/>
    </dict>
  </plist>
  ```

- [ ] **Step 4: 添加打包脚本**
  ```json
  // package.json
  {
    "scripts": {
      "dev": "electron-vite dev",
      "build": "npm run build:main && npm run build:renderer && npm run build:preload",
      "build:main": "tsc --project tsconfig.main.json",
      "build:renderer": "tsc --project tsconfig.renderer.json && vite build",
      "build:preload": "tsc --project tsconfig.preload.json",
      "preview": "electron-vite preview",
      "dist": "npm run build && electron-builder",
      "dist:win": "npm run build && electron-builder --win",
      "dist:mac": "npm run build && electron-builder --mac",
      "dist:linux": "npm run build && electron-builder --linux",
      "postinstall": "electron-builder install-app-deps"
    }
  }
  ```

---

### Task 2: 自动更新配置

**目标：** 配置自动更新功能

**文件：** `main/updater.ts`

**步骤：**

- [ ] **Step 1: 集成 electron-updater**
  ```typescript
  import { autoUpdater } from 'electron-updater';
  import { dialog, BrowserWindow } from 'electron';
  import log from 'electron-log';

  export function initAutoUpdater(mainWindow: BrowserWindow): void {
    // 配置日志
    log.transports.file.level = 'info';
    autoUpdater.logger = log;

    // 检查更新
    autoUpdater.checkForUpdatesAndNotify();

    // 监听更新事件
    autoUpdater.on('update-available', (info) => {
      dialog.showMessageBox(mainWindow, {
        type: 'info',
        title: '发现新版本',
        message: `发现新版本 ${info.version}，是否现在下载？`,
        buttons: ['下载', '稍后'],
      }).then((result) => {
        if (result.response === 0) {
          autoUpdater.downloadUpdate();
        }
      });
    });

    autoUpdater.on('update-downloaded', (info) => {
      dialog.showMessageBox(mainWindow, {
        type: 'info',
        title: '更新已下载',
        message: `新版本 ${info.version} 已下载，是否现在安装？`,
        buttons: ['安装并重启', '稍后'],
      }).then((result) => {
        if (result.response === 0) {
          autoUpdater.quitAndInstall();
        }
      });
    });

    autoUpdater.on('error', (error) => {
      log.error('Update error:', error);
      dialog.showErrorBox('更新错误', error.message);
    });
  }
  ```

---

### Task 3: 生成类型声明文件

**目标：** 生成 .d.ts 类型声明文件

**步骤：**

- [ ] **Step 1: 配置类型声明生成**
  ```json
  // tsconfig.json 中添加
  {
    "compilerOptions": {
      "declaration": true,
      "declarationMap": true,
      "declarationDir": "./types"
    }
  }
  ```

- [ ] **Step 2: 生成声明文件**
  ```bash
  npx tsc --project tsconfig.main.json --declaration --emitDeclarationOnly
  npx tsc --project tsconfig.renderer.json --declaration --emitDeclarationOnly
  ```

---

### Task 4: 编写技术文档

**目标：** 编写项目技术文档和用户使用手册

**文件：** `docs/`

**步骤：**

- [ ] **Step 1: 项目 README**
  ```markdown
  # Jima's SamplerHub

  > 音乐制作人智能采样管理工作站

  ## 功能特性

  - 📁 智能采样库管理
  - 🔍 全文搜索与高级筛选
  - 🎵 内置音频播放器与波形可视化
  - 🏷️ 智能分类与标签管理
  - 🖱️ DAW 拖拽集成
  - ⭐ 收藏夹与最近使用
  - 📦 采样库导入/导出

  ## 系统要求

  - Windows 10/11 (64位)
  - macOS 12+ (Intel/Apple Silicon)
  - 4GB RAM (推荐 8GB)
  - 500MB 磁盘空间

  ## 安装

  ### Windows
  1. 下载 `Jima's-SamplerHub-Setup-x.x.x.exe`
  2. 运行安装程序
  3. 按照向导完成安装

  ### macOS
  1. 下载 `Jima's-SamplerHub-x.x.x.dmg`
  2. 打开 DMG 文件
  3. 将应用拖到 Applications 文件夹

  ## 快速开始

  1. 启动应用
  2. 点击"添加采样文件夹"选择你的采样库目录
  3. 等待扫描完成
  4. 开始浏览和搜索你的采样！

  ## 快捷键

  | 快捷键 | 功能 |
  |--------|------|
  | Ctrl+F | 搜索采样 |
  | Ctrl+Space | 播放/暂停 |
  | Ctrl+D | 删除选中采样 |
  | Escape | 清除选择 |

  ## 技术栈

  - Electron 31
  - React 18 + TypeScript 5.4
  - Ant Design 5
  - Drizzle ORM + SQLite
  - Howler.js + wavesurfer.js

  ## 开发

  ```bash
  # 安装依赖
  npm install

  # 开发模式
  npm run dev

  # 构建
  npm run build

  # 打包
  npm run dist
  ```

  ## 许可证

  MIT License
  ```

- [ ] **Step 2: API 文档**
  ```markdown
  # API 文档

  ## IPC 通信

  ### 采样相关

  #### `samples:get`
  获取采样列表

  **请求参数：** 无

  **响应：**
  ```typescript
  {
    success: boolean;
    data: Sample[];
    error?: string;
  }
  ```

  #### `samples:search`
  搜索采样

  **请求参数：**
  ```typescript
  {
    query?: string;
    categoryId?: number;
    tagIds?: number[];
    durationMin?: number;
    durationMax?: number;
    // ... 其他筛选条件
  }
  ```

  **响应：**
  ```typescript
  {
    success: boolean;
    data: {
      items: Sample[];
      total: number;
      page: number;
      pageSize: number;
    };
    error?: string;
  }
  ```

  ### 分类相关

  #### `categories:get`
  获取分类列表

  **请求参数：** 无

  **响应：**
  ```typescript
  {
    success: boolean;
    data: Category[];
    error?: string;
  }
  ```

  ### 更多 API...
  ```

- [ ] **Step 3: 用户使用手册**
  ```markdown
  # Jima's SamplerHub 用户使用手册

  ## 目录

  1. [安装与启动](#安装与启动)
  2. [添加采样文件夹](#添加采样文件夹)
  3. [浏览采样](#浏览采样)
  4. [搜索采样](#搜索采样)
  5. [播放采样](#播放采样)
  6. [拖拽到 DAW](#拖拽到-daw)
  7. [管理标签](#管理标签)
  8. [收藏采样](#收藏采样)
  9. [导出/导入](#导出导入)
  10. [设置](#设置)

  ## 安装与启动

  ### Windows
  ...

  ### macOS
  ...

  ## 添加采样文件夹

  1. 点击左侧边栏的"添加文件夹"按钮
  2. 选择包含采样文件的文件夹
  3. 应用会自动扫描并索引所有音频文件

  ## 浏览采样

  - 使用左侧分类树浏览不同类别的采样
  - 点击采样名称可以选中
  - Ctrl+点击可以多选

  ## 搜索采样

  - 在顶部搜索框输入关键词
  - 使用高级筛选面板进行精确筛选
  - 支持按时长、采样率、BPM、调性等筛选

  ## 播放采样

  - 点击采样卡片上的播放按钮
  - 使用底部播放器控制播放
  - 支持波形可视化显示

  ## 拖拽到 DAW

  - 选中采样后，直接拖拽到 DAW 中
  - 支持 FL Studio、Studio One、Ableton Live 等
  - 支持批量拖拽多个采样

  ## 管理标签

  - 点击标签管理按钮
  - 创建新标签或选择已有标签
  - 为采样添加标签以便分类

  ## 收藏采样

  - 点击采样卡片上的星形图标
  - 收藏的采样会显示在收藏夹页面

  ## 导出/导入

  - 在设置页面选择导出采样库
  - 导出的文件可以用于备份或在其他设备上导入

  ## 设置

  - 自动扫描间隔
  - 音频播放设置
  - 界面主题
  - 快捷键配置
  ```

---

### Task 5: 数据库结构文档

**目标：** 生成数据库结构说明

**文件：** `docs/database.md`

```markdown
# 数据库结构

## 表结构

### samples（采样文件表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| file_path | TEXT | 文件绝对路径，唯一 |
| file_name | TEXT | 文件名 |
| file_size | INTEGER | 文件大小（字节） |
| file_hash | TEXT | 文件哈希值 |
| created_at | TIMESTAMP | 文件创建时间 |
| modified_at | TIMESTAMP | 文件修改时间 |
| duration | REAL | 时长（秒） |
| sample_rate | INTEGER | 采样率（Hz） |
| bit_rate | INTEGER | 比特率（bps） |
| channels | INTEGER | 声道数 |
| bpm | REAL | BPM（可选） |
| key | TEXT | 调性（可选） |
| category_id | INTEGER | 分类ID，外键 |
| waveform_data | BLOB | 波形数据 |
| is_favorite | BOOLEAN | 是否收藏 |
| play_count | INTEGER | 播放次数 |
| last_played_at | TIMESTAMP | 最后播放时间 |
| indexed_at | TIMESTAMP | 索引时间 |

### categories（分类表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| name | TEXT | 分类名称，唯一 |
| parent_id | INTEGER | 父分类ID，自关联 |
| is_system | BOOLEAN | 是否系统预设 |
| sort_order | INTEGER | 排序顺序 |

### tags（标签表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| name | TEXT | 标签名称，唯一 |
| color | TEXT | 标签颜色 |

### sample_tags（采样-标签关联表）

| 字段 | 类型 | 说明 |
|------|------|------|
| sample_id | INTEGER | 采样ID，外键 |
| tag_id | INTEGER | 标签ID，外键 |

### classification_rules（分类规则表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| name | TEXT | 规则名称 |
| pattern | TEXT | 匹配模式 |
| rule_type | TEXT | 规则类型：regex/keyword/folder |
| target_category_id | INTEGER | 目标分类ID |
| priority | INTEGER | 优先级 |
| is_active | BOOLEAN | 是否启用 |

### watched_folders（监控文件夹表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| path | TEXT | 文件夹路径，唯一 |
| last_scan_at | TIMESTAMP | 最后扫描时间 |
| is_active | BOOLEAN | 是否启用监控 |

### recent_samples（最近使用表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| sample_id | INTEGER | 采样ID，外键 |
| played_at | TIMESTAMP | 播放时间 |

## 索引

- samples.file_path (唯一索引)
- samples.category_id (索引)
- samples.is_favorite (索引)
- samples_fts (全文搜索虚拟表)
- categories.parent_id (索引)
- sample_tags.sample_id (索引)
- sample_tags.tag_id (索引)
```

---

## 阶段五验收标准

- [ ] Windows 安装包（.exe）生成成功
- [ ] macOS 安装包（.dmg）生成成功
- [ ] 安装包大小 < 200MB
- [ ] 安装过程无错误
- [ ] 应用启动正常
- [ ] 自动更新功能正常工作
- [ ] 类型声明文件完整
- [ ] README 文档完整
- [ ] API 文档完整
- [ ] 用户使用手册完整
- [ ] 数据库结构文档完整
- [ ] 所有文档无错别字
- [ ] 最终产品通过所有验收测试
